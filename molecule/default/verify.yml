---
- name: Verify
  hosts: all
  gather_facts: false
  tasks:
    - name: "Verify system is reachable"
      ansible.builtin.ping:

    - name: "Verification complete"
      ansible.builtin.debug:
        msg: "✅ Collection playbook test completed successfully!"
