# System dependencies for the execution environment
# These packages will be installed in the container

# Basic system tools
git [platform:rpm platform:dpkg]
rsync [platform:rpm platform:dpkg]
openssh-clients [platform:rpm]
openssh-client [platform:dpkg]

# Python development
python3-dev [platform:dpkg]
python3-devel [platform:rpm]

# Networking tools
curl [platform:rpm platform:dpkg]
wget [platform:rpm platform:dpkg]

# Docker support (if needed for molecule)
# docker [platform:rpm platform:dpkg]

# Additional tools that might be useful
jq [platform:rpm platform:dpkg]
